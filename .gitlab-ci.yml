image: node:24

stages:
  - build

pages:
  stage: build
  script:
    - npm ci
    - export VITE_BASE_PATH=/${CI_PROJECT_NAME}/
    - npm run build
    - rm -rf public
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
